node_modules
dist
*.local
.env
.env.*
!.env.example
# Intentionally tracked : origin trial tokens are public and the repository
# must reproduce the deployed artifact during the freeze.
!.env.production
.DS_Store
*.tsbuildinfo
*.log
.vercel
.netlify

# Local Wrangler deployment state.
.wrangler/
