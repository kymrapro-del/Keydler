# WebMCP origin trial token, obtained from https://developer.chrome.com/origintrials
# One token per deployed origin. Local development uses the browser flag :
# chrome://flags/#enable-webmcp-testing suffit.
VITE_WEBMCP_ORIGIN_TRIAL_TOKEN=
