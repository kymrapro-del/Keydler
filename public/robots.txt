# The Cloudflare zone can prefix a managed policy to this file. Verify the
# served result with `curl https://keydler.com/robots.txt` after deployment.
#
# Everything is indexable. The product serves no user data : task records live
# only in the visitor's browser, so task URLs expose only the application shell.

User-agent: *
Allow: /

Sitemap: https://keydler.com/sitemap.xml
