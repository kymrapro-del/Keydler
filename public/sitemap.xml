<?xml version="1.0" encoding="UTF-8"?>
<!--
  A single address, by design.

  `/t/:id` identifies a task that exists only in one visitor's browser. A
  crawler would find only the application shell.

  `/workspace` serves the same HTML with `/` as its canonical URL, so it is not
  listed separately.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://keydler.com/</loc>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>
